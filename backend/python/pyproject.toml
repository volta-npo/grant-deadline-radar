[project]
name = "grant-deadline-radar-backend"
version = "0.1.0"
description = "Backend evidence engine for Grant Deadline Radar"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
